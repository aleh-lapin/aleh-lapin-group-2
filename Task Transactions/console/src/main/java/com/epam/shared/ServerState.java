package com.epam.shared;

/**
 * The Enum ServerState.
 * Used as Message for Control Bus
 */
public enum ServerState {

	/** The startup. */
	STARTUP,
 /** The shutdown. */
 SHUTDOWN,
 /** The unknown. */
 UNKNOWN;
}
